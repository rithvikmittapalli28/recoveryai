# Agent Layer

This directory is reserved for bounded AI agent logic.

Do not place provider keys or browser-exposed code here. Agent implementations should depend on validated business services and explicit tool boundaries.
